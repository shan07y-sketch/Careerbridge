# Copy to .env.seedprod (gitignored) and set your REMOTE database URL.
# Railway: dashboard -> Postgres service -> "Connect" -> DATABASE_URL (public URL).
DATABASE_URL="postgresql://user:password@host:port/database"
